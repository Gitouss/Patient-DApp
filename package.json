{
  "name": "patient-dapp",
  "version": "1.0.0",
  "description": "This is a blockchain application for healthcare",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "browserify src/js/jsrp.js -o src/js/bundle.js",
    "dev": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^16.1.1",
    "lite-server": "^2.3.0"
  }
}
